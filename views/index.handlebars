<div class='p-5 mb-4 bg-light rounded-3'>
  <div class='container-fluid py-5'>
    <h1 class='display-5 fw-bold'>Bem vindo ao blog de Node</h1>
    <p class='col-md-8 fs-4'>Using a series of utilities, you can create this
      jumbotron, just like the one in previous versions of Bootstrap. Check out
      the examples below for how you can remix and restyle it to your liking.</p>
    <button class='btn btn-primary btn-lg' type='button'>Crie uma conta</button>
  </div>
</div>

<hr />

<h2 class='mb-3'>Postagens Recentes</h2>

{{#each postagens}}
  <div class='card mb-3'>
    <div class='card-body'>
      <div class='row align-items-center'>
        <h3 class='col'>{{titulo}}</h3>
        <span
          class='col-auto badge text-bg-secondary me-2'
        >{{categoria.nome}}</span>
      </div>
      <p>{{descricao}}</p>

      <a href='/postagem/{{slug}}'><button
          class='btn btn-success btn-sm'
          type='submit'
        >Leia mais</button></a>
    </div>

    <div class='card-footer text-muted'>
      <small>{{data}}</small>
    </div>
  </div>
{{else}}
  <h5>Nenhuma postagem recente</h5>
{{/each}}